import React from "react";
import { UserGraphCard } from "./UserGraphStyle";

export const UserGraph = () => {
    return (
        <UserGraphCard>
            <p>Data Graph</p>
        </UserGraphCard>
    );
};
